# HTCondor installer

The HTCondor MSI installer is not committed to Git because it is too large for a normal source-code repository.

Before running the one-click HTCondor setup, place the installer here:

third_party/htcondor/condor-Windows-x64.msi

Required version used during development:
HTCondor 25.0.11 Windows x64
